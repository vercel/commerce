query GetCollections {
  collections(channel: "default-channel", first: 100) {
    edges {
      node {
        id
        name
        slug
        description
        seoTitle
        seoDescription
      }
    }
  }
}
