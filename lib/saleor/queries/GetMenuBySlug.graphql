fragment MenuItem on MenuItem {
  id
  name
  url
  collection {
    slug
  }
  category {
    slug
  }
  page {
    slug
  }
}

query GetMenuBySlug($slug: String!) {
  menu(slug: $slug, channel: "default-channel") {
    id
    slug
    name
    items {
      ...MenuItem
      children {
        ...MenuItem
        children {
          ...MenuItem
          children {
            ...MenuItem
          }
        }
      }
    }
  }
}
