

export enum ButonType {
    primary = 'primary',
    light = 'light',
}

export enum ButtonSize {
    default = 'default',
    large = 'large',
}

export enum InputType {
    text = 'text',
    number = 'number',
}

export const KEY = {
    ENTER: 'Enter',
}
