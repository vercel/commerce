# ADR-004: Frontend Database Access
Status: Active
Context: We are moving to a microservices architecture.
Decision: The frontend (React/Next.js) must never connect directly to the Postgres database. All data fetching must route through the Python FastAPI backend.
Consequences: PRs containing raw SQL or database connection strings in the `frontend/` directory must be rejected.